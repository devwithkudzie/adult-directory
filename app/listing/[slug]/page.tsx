import {notFound} from "next/navigation"; import {Header} from "@/components/Header"; import {Footer} from "@/components/Footer"; import {listings} from "@/data/listings";
export default async function Profile({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const x=listings.find(v=>v.slug===slug); if(!x)notFound();return <><Header/><main className="container"><div className="pageHero"><div className="kicker">Verified-style demo profile</div><h1>{x.name}</h1><p className="lead">{x.city} · Adults-only profile presentation</p></div><div className="heroGrid" style={{paddingBottom:70}}><div className="heroVisual" style={{backgroundImage:`linear-gradient(180deg,transparent,rgba(13,9,12,.7)),url("${x.img}")`}}/><div className="card" style={{padding:28}}><h2>About {x.name}</h2><p className="prose">This is fictional placeholder content demonstrating the profile-detail layout. Replace it with your own lawful directory content, policies and contact workflow.</p><div className="heroActions"><button className="btn">View contact options</button><button className="btn secondary">Report profile</button></div></div></div></main><Footer/></>}
