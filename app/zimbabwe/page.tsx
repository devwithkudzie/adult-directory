import Link from "next/link"; import {Header} from "@/components/Header"; import {Footer} from "@/components/Footer";
export default function Cities(){const cs=["Harare","Bulawayo","Victoria Falls","Mutare","Gweru"];return <><Header/><main className="container"><div className="pageHero"><div className="kicker">Locations</div><h1>Zimbabwe</h1><p className="lead">Choose a city to explore the directory.</p></div><section className="section"><div className="cities">{cs.map(c=><Link className="city" href={`/zimbabwe/${c.toLowerCase().replaceAll(" ","-")}`} key={c}>{c}<div className="meta">Explore →</div></Link>)}</div></section></main><Footer/></>}
