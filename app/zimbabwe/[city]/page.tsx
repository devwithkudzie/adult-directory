import {Header} from "@/components/Header"; import {Footer} from "@/components/Footer"; import {ListingGrid} from "@/components/ListingGrid";
export default async function City({params}:{params:Promise<{city:string}>}){const {city}=await params; const title=city.split("-").map(x=>x[0].toUpperCase()+x.slice(1)).join(" ");return <><Header/><main className="container"><div className="pageHero"><div className="kicker">City directory</div><h1>{title}</h1><p className="lead">Adult-oriented directory UI demo for {title}.</p></div><section className="section" style={{paddingTop:0}}><ListingGrid/></section></main><Footer/></>}
